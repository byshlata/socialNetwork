// eslint-disable-next-line @typescript-eslint/explicit-function-return-types
export const randomNumber = function random() {
  // eslint-disable-next-line @typescript-eslint/no-magic-numbers
  return Math.floor(Math.random() * 5) + 1;
};
